version: 2

models:
    - name: fct_deep_work_tracker
      description: Fact table of deep work tracking.
      columns:
          - name: tracker_activity_id
            description: Primary Key
            tests:
                - unique
                - not_null
    - name: dim_okrs
      description: Dimension table of objectives and key results.
      columns:
          - name: key_result_id
            description: Primary Key
            tests:
                - unique
                - not_null
              